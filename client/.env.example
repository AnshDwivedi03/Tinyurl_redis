# Client Vite env vars
# Vite requires variable names to start with VITE_
# Set these in Vercel (or in local .env) for production

# Full API base including /api (for example: https://api.example.com/api)
VITE_API_BASE=https://api.example.com/api

# Public redirect base (server origin used when constructing public short links)
VITE_REDIRECT_BASE=https://api.example.com
